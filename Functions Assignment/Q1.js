// Arrow function to calculate square
const square = (num) => num * num;

// Using the function to calculate square of a number
const number = 7;
const result = square(number);

// Display the result
console.log(`The square of ${number} is ${result}`);
